<html>
    <head>
        <style>
         a {
             font-size: 14px;
             text-decoration: none;
             border-bottom: 1px solid;
             color: #008ACE;
         }

         a:hover {
             color: #CC0000;
             border-color: #f0b2b2 !important;
         }

         .myinput {
         position: absolute;
         left: 80px;
         width: 250px;
         }
        </style>
    </head>
    <body>
        <h1>DateParser test form</h1>
        <form action="/" method="GET">
            <p>
                <label for="content">Текст
                    <input list="history" name="content" class="myinput"/></label>
                <datalist id="history">
                  {% for item in history %}
                        <option value="Chrome"/>
                    </datalist>
            </p>
            <p>
                <label for="pattern">Паттерн</label>
                <input id="pattern" name="pattern" type="text" value="{{pattern}}" class="myinput" list="patterns"/>
            </p>
            <input type="submit" value="Submit"/>
        </form>
        {% if result %}
        {{ result }}
        {% endif %}
        <div style="width: 200px; border-top: 1px solid lightgray; margin-top: 5px;"></div>

        <ol style="font-size: 14px;">
        {% for hi in history %}
            <li style="height: 25px;">
                <a href="#" onclick="document.location = '/?content=' + this.innerHTML">{{ hi }}</a>
            </li>
        {% endfor %}
        </ol>
        <script>document.getElementById('content').focus();</script>
    </body>
</html>
